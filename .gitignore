# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
!.vscode/
!.vscode/settings.json
!.vscode/extensions.json

# Spike scratch (personal data, never commit)
.spike-private/

# Written by the harness, per machine. `vp check` sees it on any checkout where the harness
# has run, because the formatter reads this file and not ~/.config/git/ignore.
.claude/settings.local.json

# A slice renders to one page — derived from its markdown, regenerated, never edited
docs/planning/*/*.html
